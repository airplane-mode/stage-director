{
  "name": "stage-director",
  "version": "0.0.10",
  "description": "Simplified, sane, reducer/action/action-creator definition system for redux",
  "main": "lib/stage-director.js",
  "scripts": {
    "clean": "rm -rf lib/*",
    "lint": "eslint src && eslint test",
    "build": "npm run lint && babel src --out-dir lib",
    "prepare": "npm run build",
    "test": "npm run build && mocha"
  },
  "author": "@airshanemode and @justifyles",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.5.5",
    "@babel/core": "^7.5.5",
    "@babel/preset-env": "^7.5.5",
    "eslint": "^6.1.0",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-plugin-import": "^2.18.2",
    "mocha": "^6.2.0"
  },
  "dependencies": {
    "core-js": "^3.2.1",
    "redux": "^4.0.4"
  }
}
